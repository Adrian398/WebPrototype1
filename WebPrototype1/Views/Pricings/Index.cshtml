@model IEnumerable<WebPrototype1.Models.Pricing>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Mobilfunk.NetworkSpeed)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.URL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MobilPhoneId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Mobilfunk.NetworkSpeed)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.URL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MobilPhoneId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PricingId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PricingId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PricingId })
        </td>
    </tr>
}

</table>
